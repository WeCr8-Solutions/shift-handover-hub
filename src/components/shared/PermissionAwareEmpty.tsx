// Shared re-export so OAP/GCA + Talent surfaces all use the same empty/permission
// UX (per gca-oap-system-audit.md Pass C, F-19).
export { PermissionAwareEmpty } from "@/components/talent/PermissionAwareEmpty";
