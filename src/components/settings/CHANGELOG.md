# Settings Components Directory Changelog

## 2026-05-23

### Established local settings directory documentation baseline

What changed:

- Added `README.md` for `src/components/settings/`.
- Added `CHANGELOG.md` for `src/components/settings/`.

Why it changed:

- The phased admin-platform work needs clear boundaries between settings surfaces and admin back-office surfaces.
- The repo now requires local documentation in each actively worked directory.

Impact:

- Contributors can identify what belongs in the settings experience more quickly.
- The team now has a local place to track changes to settings architecture and why they were made.
